spring:
  datasource:
    url: jdbc:mariadb://localhost:3308/springai
    username: root
    password: root1234
  batch:
    job:
      names: ${job.name:NONE}
    jdbc:
      initialize-schema: always